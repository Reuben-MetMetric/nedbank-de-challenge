# Additional Python packages beyond the base image.
#
# Base image already includes:
#   pyspark==3.5.0
#   delta-spark==3.1.0
#   pandas==2.1.0
#   pyarrow==14.0.0
#   pyyaml==6.0.1
#   duckdb==0.10.0
#
# Do NOT re-pin these packages — version conflicts will break the build.
# Add only packages that are genuinely missing from the base image.
